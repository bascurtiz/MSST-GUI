"""backend/version.py — single source of truth for the app version.

Keep in sync with `#define AppVersion` in MSST-GUI.iss (used for the
installer's version metadata and update comparisons via release tags).
"""
APP_VERSION = "1.0.3"
