# MSST GUI — inference runtime dependencies
# Installed into the bundled runtime by the first-run GPU setup,
# AFTER the GPU-appropriate PyTorch build (see backend/runtime_setup.py).
# torch / torchaudio are intentionally NOT listed here.

# === Audio I/O & Metadata ===
mutagen>=1.47.0
soundfile>=0.12.0
librosa>=0.10.0
pyloudnorm>=0.2.0

# === Core ML ===
numpy>=1.24.0
scipy>=1.10.0

# === Imaging (torchmetrics compat) ===
# torchmetrics 1.x touches PIL.__version__ at import time (PILLOW_VERSION
# compat shim). The frozen app ships stray PIL *.pyd binaries in _internal
# (GUI bundle leftovers) that import as a namespace package without
# __version__, so any pytorch_lightning-based architecture (bandit) crashes
# with "module 'PIL' has no attribute '__version__'" unless a real Pillow is
# installed into the runtime.
Pillow>=10.0

# === Model Architectures ===
einops>=0.8.0
rotary-embedding-torch>=0.8.0
conformer>=0.3.0
spafe>=0.3.0
# PoPE positional encoding — required by PolarFormer (bs/mel roformer use_pope: True)
pope-pytorch>=0.1.0

# === Model Zoo (Demucs / Open-Unmix) ===
demucs>=4.0.0
openunmix>=1.3.0

# === Config / Serialization ===
PyYAML>=6.0
omegaconf>=2.3.0
ml-collections>=1.0.0
packaging>=23.0

# === Training & Evaluation (imported by utils/) ===
tqdm>=4.65.0
loralib>=0.1.0
wandb>=0.15.0
auraloss>=0.4.0
torch-log-wmse>=0.3.0
pytorch-lightning>=2.0.0
torchmetrics>=1.0.0
# asteroid — required by the Bandit architecture (models/bandit/core imports
# asteroid.losses at module import time, so even inference needs it).
asteroid>=0.6.0

# === Transformers (for UperNet segmentation models) ===
transformers>=4.30.0

# === Audio Augmentation ===
audiomentations>=0.33.0
pedalboard>=0.9.0

# === Networking ===
requests>=2.31.0
urllib3>=2.0.0
# urllib3 2.6+ probes a zstd codec; installing the canonical package keeps it
# away from broken stub 'zstd'/'backports.zstd' bindings (AttributeError on
# ZstdError crashed inference startup).
zstandard>=0.22.0

# === Type Checking ===
beartype>=0.15.0
